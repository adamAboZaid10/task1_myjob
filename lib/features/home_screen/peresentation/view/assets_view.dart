import 'package:flutter/material.dart';

class AssetsView extends StatelessWidget {
  const AssetsView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('assets view'),
      ),
    );
  }
}
